# Archived Build Plan Note

The original long-form build-plan note was removed from the public-facing docs to keep the repo focused on the smallest useful story:

- build the toolchain
- run a demo
- verify the behavior

The maintained implementation note that remains in the repo is:

- `docs/DETAILED_BUILD_PLAN.md`
