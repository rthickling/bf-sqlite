# Reference Examples

These files are sketches, protocol notes, or partial examples.

They are useful if you want to study the pager protocol, but they are not the main demo path.

Start with:

- `examples/01_hello_header.bf`
- `./phase5_table_scan`
- `./bin/run-tests`
